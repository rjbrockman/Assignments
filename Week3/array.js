var main = function () {
    "use strict";
	function mymax(45, 60, 33, 55){
    var max = -Infinity, i = 0, n = arr.length;

    for (; i != n; ++i) {
        if (arr[i] > max) {
            max = arr[i];
        }
    }

    return max;
}
};
$(document).ready(main);